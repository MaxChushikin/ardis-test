{% extends 'admin/admin_content_base.html.twig' %}

{% block title %}{{ text_title }}{% endblock %}

{% block body_nav %}
    <div class="container-fluid">
        <h1>{{ text_title }}</h1>
        <div class="pull-right">
            <button type="submit" form="attribute_form" class="btn btn-primary"><i class="fa fa-save"></i></button>
            <a href="{{ path('admin_attribute_list') }}" class="btn btn-secondary"><i class="fa fa-reply"></i></a>
        </div>
    </div>
{% endblock %}

{% block content_body %}
    <div class="row">
        <div class="col-sm-12">
            {{ include('admin/attribute/_form.html.twig') }}
        </div>
    </div>
{% endblock %}