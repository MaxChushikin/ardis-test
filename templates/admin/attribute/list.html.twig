{% extends 'admin/admin_content_base.html.twig' %}

{% block title %}Attributes{% endblock %}

{% block body_nav %}
    <div class="container-fluid">
        <h1>Attributes</h1>
        <div class="pull-right">
            <a href="{{ path('admin_attribute_add') }}" class="btn btn-primary"><i class="fa fa-plus"></i></a>
        </div>
    </div>
{% endblock %}

{% block content_body %}
    <div class="row">
        <div class="col-sm-12">
            <form>
                <div class="input-group mb-3">
                    <input type="text"
                           name="search"
                           value="{{ app.request.query.get('search') }}"
                           class="form-control"
                           placeholder="Search..."
                    >
                    <div class="input-group-append">
                        <button type="submit"
                                class="btn btn-outline-secondary">
                            <span class="fa fa-search"></span>
                        </button>
                    </div>
                </div>
            </form>

            <table class="table table-bordered">
                <thead>
                <tr>
                    <th width="60px" scope="col">#</th>
                    <th scope="col">Name</th>
                    <th width="120px"></th>
                </tr>
                </thead>
                <tbody>
                {% for attribute in pagination %}
                    <tr>
                        <td>{{ attribute.id }}</td>
                        <td>{{ attribute.name }}</td>
                        <td>
                            <a class="btn btn-primary" href="{{ path('admin_attribute_edit', {id: attribute.id}) }}"><span class="fa fa-edit"></span></a>
                            <a class="btn btn-danger" href="{{ path('admin_attribute_remove', {id: attribute.id}) }}" onclick="return confirm('Are you sure you want to delete {{ attribute.name }}?');"><span class="fa fa-trash"></span></a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="4" class="text-center">
                            No attributes found
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>

            {{ knp_pagination_render(pagination) }}
        </div>
    </div>
{% endblock %}